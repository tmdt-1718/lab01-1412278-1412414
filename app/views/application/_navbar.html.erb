<nav class="navbar navbar-expand-lg navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="pull-left">
                    <%=image_tag("logo.png", alt: "Logo", height: "50", width: "70")%>
                </a>
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#my_navbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#my_left_menu" aria-expanded="false">
                    <span class="glyphicon glyphicon-th-list" style="color:white"></span>
                </button>
            </div>
            <div class="collapse navbar-collapse" id="my_navbar">
                <ul class="nav navbar-nav">
                    <li class="<%= yield(:active_home) %>"><a href="/">Home</a></li>
                    <li class="<%= yield(:active_album) %>"><a href="#">Album</a></li>
                    <li class="<%= yield(:active_about) %>"><a href="#">About</a></li>
                    <li class="<%= yield(:active_blog) %>"><a href="#">Blog</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <% if session[:firstname_current_user] && session[:lastname_current_user] %>
                        <li><a>Hello, <%= session[:firstname_current_user] %> <%= session[:lastname_current_user] %></a></li>
                        <li><a href="<%= logout_path %>" data-method="delete"><span class="glyphicon glyphicon-log-in"></span> Log Out</a></li>
                    <% else %>
                        <li><a href="<%= new_user_path %>"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
                        <li><a href="<%= login_path %>"><span class="glyphicon glyphicon-log-in"></span> Log In</a></li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>